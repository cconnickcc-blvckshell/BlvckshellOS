"""Blvckshell Agent Harness — the nervous system of an autonomous organization.

The harness is the orchestration engine that turns ideas into executed realities
through a federation of specialized AI brains. It exposes six layers: intake,
routing (CKOS), the message bus, the brain registry, shared memory, and the
observer/audit log.
"""

__version__ = "0.1.0"
